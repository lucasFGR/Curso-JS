// Criar um constante com o nome e depois veriicar se o seu nome é o que está na constante
// caso true mostrar uma mensagem de saudacao no console

const nome = 'Lucas'

if (nome === 'Lucas'){
  console.log(`Seja bem vindo ${nome}`)
}